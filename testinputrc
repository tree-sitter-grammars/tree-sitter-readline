# vim:ft=readline
# hello

   # hellowagain

$include asdf ligma squid

coNTRoL-u: a
coNTRoL-U:a

set editing-mode vi         
set emacs-mode-string
# NOTE: Whitespace here ^^^^ should work


# TODO: Make sure multiline strings are not a thing
#    "\C-m'\"
# f": "asdf"
"\C-m'\"":"asdf"
  set       keyseq-timeout     0

$if version >= 7.0
    $include hello
$else
    set editing-mode emacs
$endif

#comment
#comment
#comment

$if Bash
    $include hello
$else
    set editing-mode emacs
$endif


$if version   ==      7.0
    set keyseq-timeout 234
$endif



# $if Bash
#     $if version <= 7.0
#         set keyseq-timeout 69
#     $else
#         set keyseq-timeout 129
#     $endif
# $else
#     set keyseq-timeout 56
# $endif

